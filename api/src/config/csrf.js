// // example setup, server side renderd pages
// // installs `req.csrfToken()` to use in express view to set hidden form fields
// // input.name = `_csrf`
// // <input name="_csrf" value="[csrf-token]" type="hidden">

// const csrf = require('csrf');

// const csrfMiddleware = csrf({});

// app.use(csrfMiddleware);
